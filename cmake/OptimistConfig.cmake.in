@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# List dependencies
if (NOT OPTIMIST_EXTERNAL_EIGEN)
  find_dependency(Eigen3 REQUIRED)
endif()

# Provide the target
include("${CMAKE_CURRENT_LIST_DIR}/OptimistTargets.cmake")
